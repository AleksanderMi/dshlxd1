{% extends 'base.html.twig' %}

{% block body %}
    <div>
        {{ form_start(form) }}
        {{ form_widget(form) }}
        <button type="submit"  class="btn btn-primary">{{ whatToDo }}</button>
        {{ form_end(form) }}
    </div>
    <a href="/" class="btn btn-primary" role="button">go back</a>
{% endblock %}
